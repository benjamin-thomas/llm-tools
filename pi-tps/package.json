{
  "name": "pi-tps",
  "version": "0.1.0",
  "description": "Pi extension: end-to-end token throughput per provider/model",
  "type": "module",
  "private": true,
  "license": "MIT",
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
